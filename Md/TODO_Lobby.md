# 로비 UI 및 페이즈 전환 작업 진행 상황

## 🏁 지금까지 작업한 내용
- `L_Lobby` 레벨 생성 완료
- `WBP_LobbyList` UI 생성 완료
- `Match.WaitingForPlayers` 단계의 시작 흐름(Flow) 처리

## 🚀 내일 (다음 작업) 진행할 내용
- `WBP_LobbyList` 내에 생성된 버튼 기능 연동
- 맵 로딩과 함께 `Match.Playing` 단계로 페이즈 전환 (Phase Transition) 처리 
